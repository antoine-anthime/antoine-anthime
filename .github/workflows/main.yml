# This is a basic workflow to help you get started with Actions

name: DevCard

on:
  workflow_dispatch:
  push: 
    branches: 
      - main
  schedule: 
    - cron: '0 0 * * *'

jobs: 
  devcard:
    runs-on: ubuntu-latest
    steps:
      - name: devcard
        uses: dailydotdev/action-devcard@2.0.2
        with: 
          devcard_id: ${{e7221b3ebc404059a52ad88666758216}}
